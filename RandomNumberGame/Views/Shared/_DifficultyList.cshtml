@using RandomNumberGame.Models

@Html.DropDownList("select-difficulty", 
      new SelectList(Enum.GetValues(typeof(Enums.DifficultyLevel))),
      "Select a difficulty...",
      new { @class = "form-control" })
